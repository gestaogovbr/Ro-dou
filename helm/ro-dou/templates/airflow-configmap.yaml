apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ro-dou.fullname" . }}-airflow-config
  labels:
    {{- include "ro-dou.labels" . | nindent 4 }}
    app: airflow
data:
{{- range $key, $value := .Values.airflow.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}